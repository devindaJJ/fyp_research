import ParkingDetails from './ParkingDetails';

// Re-export the updated ParkingDetails so legacy imports of SmartParking
// automatically use the new component without changing all import sites.
export default ParkingDetails;
